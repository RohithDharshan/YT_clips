// Copy this file to config.js (gitignored) and fill in your real values.
// The Google Client ID is meant to be public — it is safe to ship in
// frontend JS. Security comes from the origin allowlist you configure in
// Google Cloud Console, plus server-side verification of the ID token.

window.__GOOGLE_CLIENT_ID__ = "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com";

// Leave unset to auto-detect the ClipMind API on localhost:8000/8010/8001.
// Set this explicitly when the frontend and API are on different hosts
// (e.g. a production deployment).
// window.__API_URL__ = "https://api.yourdomain.com";
